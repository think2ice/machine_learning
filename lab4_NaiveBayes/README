In this lab session the Naive Bayes method is used to classify (supervised learning) data into different classes. Two datasets are used: identify the topic of a new and classify emails into spam or non-spam. 
